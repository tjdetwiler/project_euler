#include <stdio.h>
#include <map>
#include <set>

using namespace std;

namespace utils{

void print_matrix(int**,int);
void print_map(map<int,int>);
void print_set(set<int>);
bool map_contains(map<int,int>, std::pair<int,int>);
}

